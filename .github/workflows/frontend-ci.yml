name: Frontend CI

# Validates the SPAs compile before Frontend CD publishes them. Path-filtered to the SPAs.
on:
  pull_request:
    branches: [main]
    paths:
      - 'src/Ecommerce.Admin.Web/**'
      - 'src/Ecommerce.Shop.Web/**'
      - '.github/workflows/frontend-ci.yml'
      - '.github/actions/setup-dotnet/action.yml'

  push:
    branches-ignore: [main]
    paths:
      - 'src/Ecommerce.Admin.Web/**'
      - 'src/Ecommerce.Shop.Web/**'
      - '.github/workflows/frontend-ci.yml'
      - '.github/actions/setup-dotnet/action.yml'

# Least privilege: CI only ever reads the repository.
permissions:
  contents: read

concurrency:
  group: frontend-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: ./.github/actions/setup-dotnet

      - name: Publish backoffice
        working-directory: src
        run: dotnet publish Ecommerce.Admin.Web/Ecommerce.Admin.Web.csproj -c Release --no-restore

      - name: Publish store
        working-directory: src
        run: dotnet publish Ecommerce.Shop.Web/Ecommerce.Shop.Web.csproj -c Release --no-restore
