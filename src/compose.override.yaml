services:
  ecommerce-api:
    env_file:
      - .env
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - ${USERPROFILE}/.aspnet/https:/https:ro

  ecommerce-db:
    env_file:
      - .env
    ports:
      - "5432:5432"
